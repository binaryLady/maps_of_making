.claude/
.gemini/
.bmad/
_bmad/
node_modules/

# OS / editor
.directory
.mcp*
*.log

# Python
__pycache__/
*.pyc
*.pyo
venv/
.venv/
.env
!infra/.env


# nginx auth — real credentials on VPS, empty placeholder for local dev (never commit)
infra/nginx/.htpasswd

# Bind-mount data dirs (persisted on VPS, not in git)
data/oxigraph/*
data/nanoclaw/*
!data/oxigraph/.gitkeep
!data/nanoclaw/.gitkeep

# GeoJSON is generated at deploy time (make publish → materialize_geojson.py)
# Never commit — it is always rebuilt from Oxigraph on the VPS
web/data/spaces.geojson

VID-20251124-WA0002.mp4
scraped/whatsapp-vulca/WhatsApp Chat with Hi👋VULCA.FRIENDS🚐.txt
.env copy
archive.tar.gz
data/tasks/heartbeat_log.db
data/tasks/snapshot_store.db
data/tasks/gap_log.txt
data/tasks/oxigraph.db
